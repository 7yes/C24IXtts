<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_nav"
    app:startDestination="@id/bodyFragment">

    <fragment
        android:id="@+id/bodyFragment"
        android:name="com.example.tts01.body.login.LoginFragment"
        android:label="fragment_body"
        tools:layout="@layout/fragment_login" >
        <action
            android:id="@+id/action_bodyFragment_to_menuFragment"
            app:destination="@id/menuFragment" />
    </fragment>
    <fragment
        android:id="@+id/menuFragment"
        android:name="com.example.tts01.body.menu.MenuFragment"
        android:label="fragment_menu"
        tools:layout="@layout/fragment_menu" />
</navigation>